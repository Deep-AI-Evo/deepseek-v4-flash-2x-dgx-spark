# DeepSeek-V4-Flash DSpark kernel hardening (from dgx-spark-2-deepseek-flash-0731 09 章).
# Prevents kcompactd proactive compaction soft-lockups that (with mstflint polling)
# have been observed to NULL-deref the kernel and reboot the whole box under vLLM load.
vm.compaction_proactiveness=0
